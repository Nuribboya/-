# 실행에 필요한 패키지 (빌드할 때는 추가로: pip install pyinstaller)
# 영상 생성에는 ffmpeg 프로그램도 필요합니다 (pip 패키지 아님): winget install Gyan.FFmpeg
google-api-python-client>=2.100
APScheduler>=3.10,<4
python-telegram-bot>=21
python-dotenv>=1.0
PyYAML>=6.0
requests>=2.31
tzdata>=2024.1          # Windows에는 시간대 DB가 없어서 필요 (Asia/Seoul 등)
edge-tts>=7.0          # 무료 TTS (Microsoft Edge 음성, API 키 불필요)
